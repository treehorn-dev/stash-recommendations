name: Stash Recommendations
description: Recommendation sync plugin and local recommendation surfaces for Stash.
version: "0.1.9"
url: https://github.com/treehorn-dev/stash-recommendations
exec:
  - python3
  - "{pluginDir}/recommendations.py"
interface: raw
ui:
  javascript:
    - ui/recommendations.js
  css:
    - ui/recommendations.css
settings:
  api_key:
    displayName: API Key
    description: API key for the recommendation service.
  service_url:
    displayName: Service URL
    description: HTTPS URL for the recommendation service.
  show_remote_results:
    displayName: Show Remote Results
    description: Include remote-only recommendations when available.
    type: BOOLEAN
  allow_private_http:
    displayName: Allow Private HTTP
    description: Allow an HTTP recommendation service only at a private or Tailnet IP address.
    type: BOOLEAN
tasks:
  - name: sync-ratings
    description: Preview a full sync of explicit ratings from local Stash.
    defaultArgs:
      mode: sync-ratings
  - name: sync-ratings-confirmed
    description: Queue a full sync of explicit ratings from local Stash.
    defaultArgs:
      mode: sync-ratings
      confirmed: true
  - name: sync-engagement
    description: Preview a full sync of recorded play and o history from local Stash.
    defaultArgs:
      mode: sync-engagement
  - name: sync-engagement-confirmed
    description: Queue a full sync of recorded play and o history from local Stash.
    defaultArgs:
      mode: sync-engagement
      confirmed: true
  - name: sync-metadata
    description: Preview a metadata sync for configured Stash-box sources.
    defaultArgs:
      mode: sync-metadata
  - name: sync-metadata-confirmed
    description: Queue a metadata sync for configured Stash-box sources.
    defaultArgs:
      mode: sync-metadata
      confirmed: true
  - name: deliver-outbox
    description: Deliver pending recommendation events and snapshots.
    defaultArgs:
      mode: deliver-outbox
  - name: status
    description: Show outbox and configuration status.
    defaultArgs:
      mode: status
hooks:
  - name: capture-rating
    description: Queue scene rating hook work for later processing.
    triggeredBy:
      - Scene.Update.Post
    defaultArgs:
      mode: capture-rating
